# Claude Code 作業ログ

## 基本情報

| 項目 | 内容 |
|------|------|
| 日付 | 2026-06-04 |
| プロジェクト | Bakery Hub SaaS（高槻BASE SaaS Challenge #001） |
| 担当AI | Claude Sonnet 4.6 |
| 本番URL | https://bakery-hub-ten.vercel.app |

## やったこと

### 1. スクショ起点の UI 改善 4件（本番デプロイ済み）

- カレンダー定休日を赤色表示
  - ファイル: `src/app/shop/[storeId]/Calendar.tsx`
- 公開予約ページの合計金額表示を大きく中央配置
  - ファイル: `src/app/shop/[storeId]/BookingForm.tsx`
- 顧客一覧タップで詳細遷移しない不具合修正
  - ファイル: `src/components/SwipeToDelete.tsx`
  - 原因: pointerdown 時点で setPointerCapture していたため tap が swipe 扱いされていた
  - 対処: 6px 超移動後に capture する方式に変更
- 設定ページにスタッフ管理機能を新規追加（オーナー専用）

### 2. スタッフ管理機能の実装（フェーズ3）

| ファイル | 内容 |
|----------|------|
| `src/lib/supabase/admin.ts` | service_role を使うサーバー専用 adminClient |
| `src/app/(app)/settings/staffActions.ts` | getStaffList / createStaff / removeStaff |
| `src/app/(app)/settings/StaffManager.tsx` | オーナー専用スタッフ管理 UI |
| `src/app/(app)/settings/page.tsx` | canManageMembers ガードでセクション追加 |

- コミット: 6c7ef4f
- 本番デプロイ完了・ビルド成功確認済み

### 3. 環境変数 SUPABASE_SERVICE_ROLE_KEY

- Vercel 本番（Production + Preview）に登録済み
- ローカル `.env.local` にも登録済み
- 途中、値に誤ってウェブサイトURLを入れていたが正しい JWT 形式に修正済み

## 設計判断

- service_role キーは絶対に `NEXT_PUBLIC_` にしない（ブラウザに露出させない）
- `server-only` パッケージは未導入だが、admin.ts はサーバー専用ファイルとして管理
- handle_new_user トリガーが `user_metadata` の `join_store_id` / `role` を読んで同一店舗に紐付ける方式

## 失敗・問題

- ブラウザで `DNS_PROBE_FINISHED_NXDOMAIN` エラーが出た
  - 原因: Chrome の履歴に `vercel.xn--app...` という壊れたアドレスが残っていた
  - 対処: シークレットウィンドウで正しい URL を開いて解決。アプリ側の問題ではなかった
- 環境変数に誤った値（URL文字列）を設定してしまった
  - 対処: eyJ... から始まる正しい JWT 形式に修正

## 次回の注意事項

- スタッフ管理機能の実画面確認は石田さん本人がオーナーでログインして行うこと
  （Claude はパスワード入力の代行不可）
- デプロイ用トークンは新セッションでは石田さんに貼り直してもらう。値はメモリ/ファイルに保存しない。
- service_role キーの値は絶対にファイル・Git・メモリに書かない

## Notion 転記候補

- スタッフ管理機能の実装完了（コミット 6c7ef4f）
- SwipeToDelete の pointerCapture 修正（タップ→スワイプ誤検知の回避方法として参考になる）
